# RecallPointWeb
HomePage for RecallPoint Information

## Latest Version
**v0.26.2** (versionCode 42) — Critical fix: podcast playback now reliably reconnects when the Android media service was killed while idle. Detects stale MediaController references and triggers fresh reconnect on every episode tap.
